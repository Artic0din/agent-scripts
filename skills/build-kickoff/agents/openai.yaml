interface:
  display_name: "Build Kickoff"
  short_description: "Emit a standardized greenfield build kickoff brief (autonomous or human-gated)."
  default_prompt: "Assemble a build kickoff brief: pick autonomous implementation or human-gated review, fill the project phases and stack, and preserve the standing rules and existing merge authorization."

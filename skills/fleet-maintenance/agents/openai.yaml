interface:
  display_name: "Fleet Maintenance"
  short_description: "Disabled until Ryan's Mac fleet is configured"
  default_prompt: "Use $fleet-maintenance to check whether Ryan's fleet workflow is configured; stop while it remains disabled."
